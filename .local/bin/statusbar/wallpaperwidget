#!/bin/sh

# Prints the current volume or 🔇 if muted.

case $BLOCK_BUTTON in
	1) wallpaper.sh ;;
	3) notify-send "📢 Wallpaper module" "\- Click to change another wallpaper." ;;
	6) "$TERMINAL" -e "$EDITOR" "$0" ;;
esac

	icon="😭"

echo "$icon"
