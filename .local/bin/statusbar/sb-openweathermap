#!/bin/sh

case $BLOCK_BUTTON in
	1) setsid weatherup ;;
	3) notify-send "🌈 Weather module" "\- Left click to update forecast." ;;
	6) "$TERMINAL" -e "$EDITOR" "$0" ;;
esac

cat "${XDG_DATA_HOME:-$HOME/.local/share}/weatherup.tmp" 2>/dev/null
